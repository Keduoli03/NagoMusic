<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Theme applied to the Android Window while the process is starting when the OS's Dark Mode setting is off -->
    <style name="LaunchTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <!-- Show a splash screen on the activity. Automatically removed when
             the Flutter engine draws its first frame -->
        <item name="android:windowBackground">@drawable/launch_background</item>
    </style>
    <!-- Theme applied to the Android Window as soon as the process has started.
         This theme determines the color of the Android Window while your
         Flutter UI initializes, as well as behind your Flutter UI while its
         running.

         This Theme is only used starting with V2 of Flutter's Android embedding. -->
    <style name="NormalTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowBackground">?android:colorBackground</item>
    </style>
    <!-- uCrop screen. Without an explicit non-fullscreen theme, the toolbar
         sits under the status bar / camera cutout on modern phones so the
         confirm / cancel icons become nearly untappable.

         父主题必须是 NoActionBar：uCrop 在 setupAppBar() 里调 setSupportActionBar()
         塞自己的 Toolbar，而带 ActionBar 的主题已经由 window decor 提供了一个，
         两者冲突会直接抛 IllegalStateException 崩掉整个进程
         （"This Activity already has an action bar supplied by the window decor"）。
         下面两个 item 是冗余的保险，防止 OEM 的主题覆盖又把 ActionBar 塞回来。 -->
    <style name="AppTheme.UCrop" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowFullscreen">false</item>
        <item name="android:fitsSystemWindows">true</item>
        <item name="android:statusBarColor">#212121</item>
        <item name="android:navigationBarColor">#212121</item>
        <item name="colorPrimary">#212121</item>
        <item name="colorPrimaryDark">#212121</item>
        <item name="colorAccent">#FFFFFF</item>
    </style>
</resources>
